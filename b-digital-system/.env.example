# B Digital system — environment variables.
# Copy this file to ".env" and fill in the values for LOCAL testing.
# (NEVER commit .env — it is git-ignored.)
# In GitHub, add these same values under: Settings → Secrets and variables → Actions.

# --- Notion (the CRM) ---
NOTION_TOKEN=
NOTION_LEADS_DB=
NOTION_CONTENT_DB=

# --- Gemini (free AI for scoring + auto copywriting) ---
GEMINI_API_KEY=

# --- Brevo (free email: notify Brian, auto-reply, gentle outbound) ---
BREVO_API_KEY=
BREVO_SENDER=brianisaacs533@gmail.com

# --- Business contact (used for WhatsApp wa.me links) ---
WHATSAPP_NUMBER=+264814794693
